User-agent: *
Allow: /

# The back office is a login, not content. Keep it out of every index.
Disallow: /admin

# Now served from the origin root (Cloudflare Workers), so this file is
# authoritative. Update the Sitemap host when the custom domain lands —
# same edit as public/llms.txt and SITE_ORIGIN in astro.config.mjs.
Sitemap: https://vidapiena.workers.dev/sitemap-index.xml
